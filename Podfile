# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GitToday' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GitToday
  pod 'SwiftSoup'
  pod 'FSCalendar'
  pod 'RxSwift'
  pod 'RxCocoa'
  target 'GitTodayTests' do
    inherit! :search_paths
    # Pods for testing
  end
  target 'GitToday Widget' do
    inherit! :search_paths
  end
  target 'GitTodayUITests' do
    # Pods for testing
  end

end
